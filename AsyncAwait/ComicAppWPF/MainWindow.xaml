<Window x:Class="ComicAppWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComicAppWPF"
        mc:Ignorable="d"
        Title="Comic Center" Height="350" Width="600" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="previousImageButton" Padding="10" Margin="15" Click="previousImageButton_Click">Previous</Button>
            <Button x:Name="nextImageButton" Padding="10" Margin="15" Click="nextImageButton_Click">Next</Button>
        </StackPanel>
        <Image Grid.Row="1" x:Name="comicImage" Margin="20" />
    </Grid>
</Window>
